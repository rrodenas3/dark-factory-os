[project]
name = "dark-factory-orchestration"
version = "0.1.0"
description = "LangGraph supervisor and specialist orchestration"
requires-python = ">=3.12"
dependencies = [
  "pydantic>=2.10.4",
  "dark-factory-governance",
  "dark-factory-tool-adapters",
]

[tool.uv]
package = true
