[project]
name = "dark-factory-governance"
version = "0.1.0"
description = "Governance contracts, risk registry, approvals, and policy controls"
requires-python = ">=3.12"
dependencies = ["pydantic>=2.10.4", "pyyaml>=6.0.2"]

[tool.uv]
package = true
