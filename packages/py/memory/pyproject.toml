[project]
name = "dark-factory-memory"
version = "0.1.0"
description = "SSGM-inspired memory and retrieval"
requires-python = ">=3.12"
dependencies = [
  "pydantic>=2.10.4",
]

[tool.uv]
package = true
