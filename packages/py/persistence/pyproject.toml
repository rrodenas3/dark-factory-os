[project]
name = "dark-factory-persistence"
version = "0.1.0"
description = "Postgres repositories for Dark Factory OS control plane"
requires-python = ">=3.12"
dependencies = [
  "asyncpg>=0.30.0",
  "pydantic>=2.10.4",
]

[tool.uv]
package = true
