[project]
name = "dark-factory-artifacts"
version = "0.1.0"
description = "Artifact schemas for agent-human collaboration"
requires-python = ">=3.12"
dependencies = ["pydantic>=2.10.4", "pyyaml>=6.0.2"]

[tool.uv]
package = true
