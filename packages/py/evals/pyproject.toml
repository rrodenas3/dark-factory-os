[project]
name = "dark-factory-evals"
version = "0.1.0"
description = "CLEAR eval harness"
requires-python = ">=3.12"
dependencies = ["pydantic>=2.10.4"]

[project.scripts]
dfos-validate-evals = "dark_factory_evals.validate:main"

[tool.uv]
package = true
